apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.squareup:javapoet:1.10.0'
    compile project(':boxfit')
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
